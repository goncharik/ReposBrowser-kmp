package com.honcharenko.reposbrowser.di

import org.koin.dsl.module

/**
 * Koin module for ViewModel dependencies.
 * This is a placeholder for future ViewModels.
 *
 * ViewModels will be added in the next phase:
 * - SearchViewModel
 * - RepoDetailsViewModel
 * - FavoritesViewModel
 */
val viewModelModule = module {
    // ViewModels will be added here in the next phase
    // Example:
    // viewModel { SearchViewModel(gitHubRepository = get(), favoritesRepository = get()) }
}
